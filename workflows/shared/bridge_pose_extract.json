{
  "load_image": {
    "class_type": "LoadImage",
    "inputs": {
      "image": "{{SOURCE_IMAGE}}"
    }
  },

  "pose_detector": {
    "class_type": "OpenposePreprocessor",
    "inputs": {
      "detect_hand": "enable",
      "detect_body": "enable", 
      "detect_face": "enable",
      "resolution": 1024,
      "image": ["load_image", 0]
    }
  },

  "save_image": {
    "class_type": "SaveImage",
    "inputs": {
      "filename_prefix": "continuum/pose",
      "images": ["pose_detector", 0]
    }
  }
}